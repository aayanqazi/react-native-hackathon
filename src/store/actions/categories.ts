import { categoriesSlice } from "../reducers/categories";

export const {
  addCategory,
  addNewFeild,
  removeFeild,
  removeCategory,
  updateCategoryName,
  updateFields,
  updateTitleField,
} = categoriesSlice.actions;
